import * as React from 'react';
import { View } from 'react-native';
import { Text } from 'galio-framework';

export const NotificationScreen = () => {
  return (
    <View style={{ flex: 1 }}>
      <Text size={20}> Notificacion en Especifico</Text>
    </View>
  );
};
